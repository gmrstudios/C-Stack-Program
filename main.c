#include "stack.h"

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
    printf("Hello, World!\n");

data_stack_t* stack = init_stack();

  int a = 6;
  int b = 7;
  int c = 33;
  int d = 12;
  int e = 3;

	push(stack, &a);
  push(stack, &b);
  push(stack, &c);
  push(stack, &d);
  push(stack, &e);









}
